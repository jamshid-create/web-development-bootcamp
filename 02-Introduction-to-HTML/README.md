# 02 - Introduction to HTML

## 📚 Topics Covered

* Introduction to HTML and its role in web development
* HTML heading elements (`<h1>` – `<h6>`)
* HTML paragraph elements (`<p>`)
* Line breaks (`<br>`)
* Horizontal rules (`<hr>`)
* Void elements

## 🧩 Projects

### 2.4 - Movie Ranking Project

A simple HTML project that combines the concepts learned in this section to create a basic movie ranking webpage.

## 💡 What I Learned

* Understanding what HTML is and its role in creating web pages
* How to use different heading levels with `<h1>` to `<h6>`
* How to create paragraphs using the `<p>` element
* How to create line breaks using `<br>`
* How to add horizontal rules using `<hr>`
* Understanding void elements and how they differ from regular HTML elements
* How to combine basic HTML elements to structure a simple webpage

## 📁 Structure

```text
02-Introduction-to-HTML/
├── 2.1-Heading-Element/
├── 2.2-Paragraph-Element/
├── 2.3-Void-Elements/
└── 2.4-Movie-Ranking-Project/
```
